import {applyAndRegister, reactive, startReactiveDom} from "./reactive.js";
/*


let colonne = reactive( {
    idColonne: "",
    listeCartes: [],

    ajouterCarte: function (carte) {
        this.listeCartes.push(carte);
    },

    suppressionCarte: function (idCarte) {
        this.listeCartes = this.listeCartes.filter(carte => carte.idCarte !== idCarte);
    },
    
    afficherToutesCartes: function () {
        return this.listeCartes;
    }

}, "colonne" + this.idColonne);

applyAndRegister(() => {});

startReactiveDom();

 */