<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block page_title %}Trollé{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset("../ressources/css/navstyle.css") }}"> <!--TODO into asset-->
    <link rel="stylesheet" href="{{ asset("../ressources/css/trellostyle.css") }}">  <!--TODO into asset-->
    <link rel="stylesheet" href="{{ asset("../tests/tests.css") }}">  <!--TODO into asset-->

</head>

<body>
<header>
    <nav>
        <ul>
            <li>
                <a href="{{ route('accueil') }}">Accueil</a>  <!--TODO into route-->
            </li>
            {% if idUtilisateurConnecte is null %}

            <li>
                <a href="{{route("afficherFormulaireConnexion")}}">  <!--TODO into asset-->
                    Connexion <img alt="login" src="{{ asset("../ressources/img/enter.png") }}" >
                </a>
            </li>
            <li>
                <a href="{{ route('afficherFormulaireCreation') }}">  <!--TODO into route-->
                    Inscription <img alt="S'inscrire" title="S'inscrire" src="{{ asset("../ressources/img/add-user.png") }}" >  <!--TODO into asset-->
                </a>
            </li>
            {% else %}

            <li>
                <a href="{{ route('afficherListeMesTableaux')}}">Mes tableaux</a>
            </li>
            <li>
                <a href="{{ route('afficherDetail',{"login" : idUtilisateurConnecte}) }}">  <!--TODO into route-->
                    Mon compte (<span>{{ idUtilisateurConnecte }}</span>) <img alt="logout" src="{{ asset("../ressources/img/user.png") }}">  <!--TODO into asset-->
                </a>
            </li>
            <li>
                <a href="{{ route('deconnexion') }}"> <!--TODO into route-->
                    Déconnexion <img alt="logout" src="{{ asset("../ressources/img/logout.png") }}"> <!--TODO into asset-->
                </a>
            </li>
            {% endif %}
            <?php %} ?>
        </ul>
    </nav>
    <div>

        {% for type in ['success','info', 'warning', 'danger'] %}
            {% for messageFlash in messagesFlash.lireMessages(type) %}
            <div class="alert alert-{{ type }}">{{ messageFlash }}</div>
            {% endfor %}
        {% endfor %}
    </div>
</header>
<main>
    {% block page_content %}
    {% endblock %}
</main>

<footer>
    <p>
        Copyright Trello-Trollé Company
    </p>
</footer>
</body>
</html>